<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= work ? work.title : 'Work error'%></title>

    <link rel='stylesheet' href='/css/style.css'/>
    <link rel="icon"
          href="/favicon.webp">

    <meta name="description" content="<%=_lt.get('acquaintance-t-1')%>"/>
    <meta name="keywords" content="разработчик, development, portfolio, портфолио, web, web-design" />

    <meta property="og:url"                content="this" />
    <meta property="og:title"              content="<%=_lt.get('acquaintance-h')%>" />
    <meta property="og:description"        content="<%=_lt.get('acquaintance-t-1')%>" />
    <meta property="og:image"              content="/images/illustration.svg" />
    <meta property="og:locale"             content="ru_RU" />
    <meta property="og:locale:alternate"   content="en_US" />

</head>
<body>
<%- include ('header.ejs'); %>

<% if (work) { %>
<section class="one-work-page">
    <h1><%= work.title ? work.title : '' %></h1>
    <div class="page">
        <% if (work.mainimage) { %>
        <a href="/images/screenshots/<%= work.mainimage %>" class="main-img"><img src="/images/screenshots/<%= work.mainimage %>" alt="preview" ></a>
        <% } %>
        <p><b><%=_lt.get('op-work-1')%></b> <%= work.name ? work.name : '' %></p>
        <p><b><%=_lt.get('op-work-2')%></b> <%= work.technologies ? work.technologies : '' %></p>
        <% if (work.gitname && work.git) {%><p><b><%=_lt.get('op-work-8')%></b> <a href="<%= work.git ? work.git : '' %>" target="_blank"><%= work.gitname ? work.gitname : '' %></a></p><%}%>
        <p><b><%=_lt.get('op-work-3')%></b> <%= work.design ? work.design : '' %></p>
        <p><b><%=_lt.get('op-work-4')%></b> <%= work.sale ? work.sale : '' %></p>
        <p><b><%=_lt.get('op-work-5')%></b><%= work.description ? work.description : ''%></p>
        <% if (work.images) { for (let i = 0; i < work.images.length; i++) { %>
        <a href="/images/screenshots/<%= work.images[i] %>" class="page-img"><img src="/images/screenshots/<%= work.images[i] %>" alt="preview" ></a>
        <% } } %>
        <p><%- work.other ? work.other : ''%></p>
        <% if (work.href && work.href !== '#') { %>
        <div class="show-site"><a href="<%= work.href %>"><%= work.href.includes("github") ? "GITHUB" : _lt.get('op-work-7')%></a></div>
        <% } %>
        <div class="back-page"></div>
    </div>
</section>
<% } %>

<%- include ('footer.ejs'); %>
</body>
</html>